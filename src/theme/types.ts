export type ColorScale = {
  primary: string;
  textPrimary: string;
  surface: string;
  surfaceAlt: string;
  border: string;
  success: string;
  danger: string;
  warning: string;
};
export type WorkflowTheme = { radius: number; spacing: number; colors: ColorScale; };
